# 自分用メモ（案内）

**やり方のメモは、フォルダに分けて置きました。**

右の一覧で **`00_ももさん用`** を開いて、  
`01_まずここ` から番号順に、必要なファイルだけ開いてください。

**付箋に貼るマニュアル（終了時／再開時）**  
→ `00_ももさん用` の **`付箋用_終了時と再開時.txt`**

（この `docs` フォルダは、古い場所から来たとき用の案内です。）
